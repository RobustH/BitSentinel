# Design: BitSentinel — 币圈合约监控与量化 Web 应用

Generated by /office-hours on 2026-05-02
Branch: master
Repo: BitSentinel
Status: APPROVED
Mode: Builder

## Problem Statement

做一个个人使用、可部署到服务器 24h 常驻运行的币圈合约监控与量化 Web 应用，专注币安合约。核心是三周期策略信号监控 + 资金流分析 + LLM 辅助判断 + 实时推送。现有工具（TradingView、freqtrade 等）要么不支持资金流+LLM 联动，要么策略定义不够灵活。

## What Makes This Cool

三周期均线策略 + 资金流分析 + LLM 分析师的组合，市面上没有工具同时做到。当多个周期同时出现首次交叉时，系统先用交易/持仓周期筛选器判断真正应该跟踪的主周期，再让 LLM 读取实时资金流数据给出"多/空/观望"建议和理由——像有个分析师坐在旁边。实时仪表盘 + 推送通知双通道，不盯屏也能收到信号。

## 核心策略逻辑

策略不是简单的指标叠加，而是一个有明确过滤链的多周期策略：

### 指标参数（均可自定义）

| 指标 | 默认参数 | 说明 |
|------|---------|------|
| EMA | 9, 21, 55 | 三条均线，用于金叉/死叉检测和排列判断。用户可在配置中自定义周期 |
| MACD | 12, 26, 9 | 标准 MACD 参数。用户可自定义快线/慢线/信号线周期 |
| KDJ | 9, 3, 3 | 标准 KDJ 参数。用户可自定义 K/D/J 周期 |

参数存储在 PostgreSQL `strategy_instances` / `strategy_templates` 表中，策略引擎启动时读取，支持热加载。

### 策略模板/骨架系统

系统不能只写死一套策略，而要先提炼出可复用的多周期趋势交易模板。用户后续可以把不同信号条件填进模板，形成一个具体的监控策略，再把这个监控策略挂载到币种和市场上运行。

核心抽象：

```
策略模板 Strategy Template
    ├── 周期槽位 Timeframe Slots
    │   ├── 方向周期 direction_tf / macro_tf
    │   ├── 结构周期 structure_tf / holding_tf
    │   ├── 触发周期 trigger_tf / execution_tf
    │   └── 可自定义更多语义槽位
    ├── 条件组 Condition Groups
    │   ├── 入监控条件 watch_conditions
    │   ├── 触发条件 trigger_conditions
    │   ├── 无效化条件 invalidate_conditions
    │   └── 出场提醒条件 exit_conditions
    ├── 信号输出 Signal Output
    │   ├── 强信号 / 弱信号 / 观察信号
    │   └── 多头 / 空头 / 观望
    └── 参数 Parameters
        ├── EMA/MACD/KDJ 参数
        ├── 周期间隔规则
        └── 推送与冷却规则
```

#### 策略模板生命周期

1. **创建模板**：定义这是 2 周期策略、3 周期策略，或者更多周期策略
2. **配置周期槽位**：每个槽位有业务含义，例如方向周期、结构周期、触发周期，而不是只按大小命名
3. **填入信号条件**：例如大周期 EMA 多头排列、小周期 EMA 金叉、MACD 零轴确认、资金流支持
4. **生成监控策略**：模板 + 参数 + 周期组合 = 一个可运行的策略实例
5. **挂载市场监控**：把策略实例绑定到 BTCUSDT/ETHUSDT 等币种
6. **运行状态机**：从未命中 → 列入监控 → 等待触发 → 触发信号 → 失效/冷却

#### 示例：双周期双均线趋势策略

```
模板名称：双周期双均线趋势跟随

周期槽位：
- trend_tf：大周期，用户可选 4h / 1d
- trigger_tf：小周期，用户可选 15m / 1h

入监控条件：
- trend_tf EMA_fast > EMA_slow
- trend_tf EMA_fast 斜率向上

触发条件：
- trigger_tf EMA_fast 上穿 EMA_slow
- trigger_tf 交叉发生在最近 N 根 K 线内

无效化条件：
- trend_tf EMA_fast < EMA_slow
- trigger_tf 金叉后未延续，重新死叉

信号输出：
- 大周期多头 + 小周期金叉 = 多头观察/入场提醒
```

#### 示例：三周期趋势确认策略

```
模板名称：三周期趋势确认

周期槽位：
- direction_tf：方向周期，例如 1d
- structure_tf：结构/持仓周期，例如 4h
- trigger_tf：触发/执行周期，例如 1h

入监控条件：
- direction_tf EMA 多头排列
- structure_tf 首次金叉或趋势转多

触发条件：
- trigger_tf 金叉
- 资金流支持多头
- MACD 动量未过热

无效化条件：
- direction_tf 转空
- structure_tf EMA 排列破坏
- 资金流明显反向
```

#### 示例：大周期方向 + 中周期震荡末期 + 小周期触发

这个是更贴近真实趋势交易的三周期模板：大周期只负责方向，中周期负责判断行情是否接近震荡末期/蓄势完成，小周期负责给出多头触发。

```
模板名称：三周期蓄势突破

周期槽位：
- direction_tf：大周期，例如 1d / 4h
- structure_tf：中周期，例如 4h / 1h
- trigger_tf：小周期，例如 1h / 15m

入监控条件：
- direction_tf EMA 多头排列
- direction_tf EMA_fast 斜率向上

结构条件：
- structure_tf 处于震荡末期
- structure_tf ATR 收缩后开始放大
- structure_tf EMA 缠绕后重新发散
- structure_tf MACD 柱体接近零轴并准备扩张

触发条件：
- trigger_tf EMA_fast 上穿 EMA_slow
- trigger_tf MACD 上穿零轴或柱体连续扩张
- trigger_tf 成交量放大

确认条件：
- 资金流支持多头
- OI 增加
- taker buy/sell ratio 偏多

信号输出：
- 大周期多头 + 中周期震荡末期 = 列入监控
- 小周期多头触发 + 资金流确认 = 强信号
- 小周期触发但资金流不支持 = 观察信号
```

#### 条件积木库

V1 不做复杂可视化拖拽，但要把常用条件抽成可组装的信号积木。一个策略模板可以同时使用指标信号、资金流信号、周期信号、风险信号和时间条件。

| 条件大类 | 条件类型 | 示例 |
|---------|---------|------|
| 指标信号 | 均线条件 | EMA9 > EMA21、EMA9 上穿 EMA21、EMA 多头排列、均线斜率向上 |
| 指标信号 | 动量条件 | MACD 上穿零轴、MACD 柱体扩张、KDJ 金叉、RSI 强弱区间 |
| 指标信号 | 波动条件 | ATR 放大、布林带突破、振幅超过阈值 |
| 结构信号 | 震荡/蓄势 | EMA 缠绕、ATR 收缩、布林带收口、MACD 靠近零轴、成交量萎缩 |
| 结构信号 | 震荡末期 | ATR 收缩后拐头、均线重新发散、布林带开口、MACD 柱体由弱转强 |
| 资金流信号 | 资金费率 | 资金费率上升、资金费率极端、费率与价格背离 |
| 资金流信号 | 主动买卖 | taker buy/sell ratio 偏多或偏空、主动买入占优 |
| 资金流信号 | 多空结构 | 大户多空比偏多、全市场多空比偏空、散户/大户分歧 |
| 资金流信号 | 持仓量 OI | OI 增加、OI 减少、价格上涨且 OI 增加、价格上涨但 OI 下降 |
| 周期信号 | 多周期确认 | 大周期确认、小周期触发、相邻周期同向、相邻周期冲突 |
| 时间条件 | 触发窗口 | 最近 N 根 K 线内触发、信号冷却 N 分钟、只在指定时间段监控 |
| 风险条件 | 风险过滤 | ATR 异常、KDJ 过热、资金费率极端、成交量不足 |

#### 条件组装规则

条件积木按阶段组织，不同阶段的意义不同：

```
入监控条件 watch_conditions
    用来判断某个币种是否进入观察池

触发条件 trigger_conditions
    用来判断是否产生提醒

确认条件 confirm_conditions
    用来提高信号等级，例如资金流支持、大周期同向

无效化条件 invalidate_conditions
    用来取消已经进入监控的候选机会

出场提醒条件 exit_conditions
    用来提示趋势可能结束
```

V1 条件组合规则建议：
- 同一阶段默认使用 `AND`，即全部满足才进入下一阶段
- 每个阶段允许配置可选条件，例如"满足 2/3 个确认条件即可升级为强信号"
- 条件积木内部保存为 JSON，前端用表单选择条件类型、周期槽位、指标参数和阈值

#### 示例：指标 + 资金流混合策略

```
模板名称：双周期趋势 + 资金流确认

入监控条件：
- trend_tf EMA9 > EMA21
- trend_tf EMA9 斜率向上

触发条件：
- trigger_tf EMA9 上穿 EMA21

确认条件：
- 资金费率不过热
- taker buy/sell ratio 偏多
- OI 增加

无效化条件：
- trend_tf EMA9 < EMA21
- 资金流明显转空

信号输出：
- 只满足指标 = 观察信号
- 指标 + 资金流同时满足 = 强信号
```

#### 策略批量挂载

一个监控策略实例可以挂载到 N 个币种上运行，而不是每个币种都单独建一套策略。

```
策略实例：双周期趋势 + 资金流确认
挂载币种：
- BTCUSDT
- ETHUSDT
- SOLUSDT
- BNBUSDT

每个币种独立维护：
- 策略状态
- 最近一次入监控时间
- 最近一次触发时间
- 周期筛选结果
- 冷却状态
- 推送记录
```

挂载规则：
- 同一策略实例可以批量绑定多个币种
- 每个币种可以单独启用/暂停
- 每个币种可以继承策略默认参数，也可以覆盖部分参数，例如周期组合、推送等级、冷却时间
- 系统按 `strategy_instance_id + symbol` 维护独立运行状态，避免 BTC 的触发影响 ETH 的监控状态

### 策略过滤链

1. **均线首次金叉/死叉扫描** → 收集候选周期
   - "首次"定义：EMA快线 与 EMA慢线 在经过至少 N 根 K 线（N 可配置，默认 10）分离后，发生的第一次交叉
   - 算法：从用户配置的监控周期列表中扫描所有周期，不再默认选择最小周期
   - 多头：EMA快线 上穿 EMA慢线（首次金叉）；空头：EMA快线 下穿 EMA慢线（首次死叉）
   - 输出：`candidate_timeframes`，包含每个候选周期的方向、交叉时间、交叉新鲜度、EMA 斜率、MACD/KDJ 状态、资金流快照
2. **交易/持仓周期筛选器** → 从候选周期中选出真正主周期
   - 解决 1h、4h、1d 等多个周期同时首次交叉导致的周期混乱问题
   - 综合评分：资金流支持度、EMA 排列质量、MACD 阶段、KDJ 位置、成交量/持仓量变化、相邻周期一致性、周期噪声惩罚
   - 输出唯一 `holding_timeframe`（持仓/主跟踪周期）和可选 `execution_timeframe`（执行/观察周期，通常为主周期下一级）
3. **大周期方向确认** → 对主周期做趋势确认
   - `holding_timeframe` 上方 1-2 个级别的周期（从用户配置的周期列表中取），EMA 排列方向应与交易方向一致
   - 如果大周期不支持但资金流强支持，则降级为观察信号，不标记为强信号
4. **主周期交叉后** → 寻找 MACD 穿零轴后的第一个反向柱作为入场提醒
   - 多头：MACD 柱状图从负转正后，观察第一轮正柱衰减和回踩确认
   - 空头：MACD 柱状图从正转负后，观察第一轮负柱扩张和反抽确认
   - V1 只推送提醒，不自动执行交易
5. **相邻周期监控** → 监控主周期上下各一个级别
   - 如果相邻小周期出现反向信号，降低当前信号强度，等待主周期二次确认
   - 如果相邻大周期方向反转，当前主周期信号降级（仅推送，不标记为强信号）
6. **无效化规则** → 均线排列被破坏
   - "均线排列被破坏"定义：多头排列（EMA9 > EMA21 > EMA55）中任一不等式不成立
   - 在 MACD 归零轴确认过程中，如果主周期 EMA 排列被破坏 → 该周期作废，重新从步骤 1 开始寻找

### 交易/持仓周期筛选器

这个模块的职责是判断"这次信号到底应该按哪个周期交易/持仓"，避免 1h、4h、1d 同时首次交叉时被最小周期或最大周期误导。

#### 输入
- `candidate_timeframes`：所有发生首次金叉/死叉的周期候选
- K 线与指标：EMA9/21/55、EMA 斜率、MACD、KDJ、ATR、成交量
- 资金与持仓：资金费率、taker buy/sell ratio、top trader long/short ratio、global long/short ratio、open interest 变化
- 上下文：用户配置的监控周期顺序、最近信号记录、当前是否已有持仓方向（V1 可手动录入）

#### 评分维度

| 维度 | 权重建议 | 说明 |
|------|---------|------|
| 资金流支持度 | 30% | 资金费率趋势、主动买卖比例、大户多空比、持仓量变化是否支持当前方向 |
| 趋势结构质量 | 25% | 主周期与上级周期 EMA 排列、EMA 斜率、交叉后的排列稳定性 |
| 动量阶段 | 20% | MACD 零轴位置、柱状图扩张/衰减、KDJ 是否过热或刚启动 |
| 周期一致性 | 15% | 相邻小周期是否先行同向，上级周期是否确认，是否存在方向冲突 |
| 噪声与风险惩罚 | 10% | 1m/5m 等小周期噪声、ATR 异常放大、成交量不足、交叉过旧 |

#### 决策规则
- 如果多个周期同向，优先选择"资金流支持 + 上级趋势确认 + 动量未过热"的周期作为 `holding_timeframe`
- 如果 1h 与 4h 同向，但 1d 未确认：通常选择 4h 为持仓周期，1h 为执行/观察周期
- 如果 4h 与 1d 同向，且资金流强支持：选择 1d 为持仓周期，4h 为执行/观察周期
- 如果小周期同向但大周期反向：不升级为强信号，只推送"短线观察"
- 如果周期之间方向冲突且资金流不支持任何一边：输出 `NO_TRADE / 观望`

#### 输出

```json
{
  "symbol": "BTCUSDT",
  "direction": "long",
  "holding_timeframe": "4h",
  "execution_timeframe": "1h",
  "confidence": 0.78,
  "decision": "TRADE_WATCH",
  "score_breakdown": {
    "money_flow": 0.82,
    "trend_structure": 0.76,
    "momentum_phase": 0.71,
    "timeframe_consensus": 0.80,
    "noise_penalty": -0.10
  },
  "alternatives": ["1h", "1d"],
  "reason": "4h 首次金叉获得 1h 先行确认，资金流同向，大周期未反向。"
}
```

### 出场信号（V1 简化版）

- 反向均线交叉：`holding_timeframe` 的 EMA9 与 EMA21 反向交叉
- 大周期方向反转：大周期 EMA 排列方向与持仓方向相反
- MACD 反向零轴穿越：MACD 从正转负（多头出场）或从负转正（空头出场）
- 出场信号不自动执行，仅推送通知

### 风险管理（V1 延迟实现）

V1 不包含自动止损/止盈/仓位管理。这些参数在 V2 实现半自动下单时加入。V1 阶段 LLM 分析会提醒风险，但不执行风控。

主要指标：EMA（默认 9,21,55 可自定义）、MACD（默认 12,26,9 可自定义）、KDJ（默认 9,3,3 可自定义）+ 资金流数据

### 监控周期（可自定义）

用户可在配置中自定义监控的 K 线周期列表。默认为 `["1m", "5m", "15m", "1h", "4h", "1d"]`。

支持所有币安合约 K 线周期：1m, 3m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 8h, 12h, 1d, 3d, 1w, 1M。

策略引擎的"候选周期扫描"、"交易/持仓周期筛选"和"大周期确认"逻辑基于用户配置的周期列表按顺序计算，不依赖固定的周期级别。

## Constraints

- 个人使用，不需要多用户
- V1 只做币安合约
- 策略用 Python 代码定义（不用可视化拖拽）
- LLM 用国内云端 API（DeepSeek/通义千问/智谱GLM）
- Web 应用，需要部署到服务器 24h 常驻运行
- V1 单用户使用，但必须有登录保护，避免暴露交易所/LLM 配置和信号数据
- 推送方式：V1 短信 + 邮箱，电话延迟到 V2

## Premises

1. **Web 应用是正确形态** — 监控任务必须脱离个人电脑，在服务器 24h 常驻运行；浏览器只负责查看、配置和手动触发分析
2. **三周期 + 资金流是正确信号组合** — 这是用户实际交易的策略，经过验证
3. **LLM 作为分析顾问** — 信号触发后读取资金流+持仓数据，给出方向建议+理由，人不做盲从
4. **服务端主存储要升级为 PostgreSQL + TimescaleDB** — K 线、指标、资金费率、资金流、持仓量属于时间序列数据，使用 TimescaleDB hypertable；策略模板、挂载关系、状态机、信号、推送日志使用 PostgreSQL 普通表。Feather/Parquet 只作为导出、冷归档或离线回测格式，不作为 V1 主库。
5. **V1 只做币安够用** — 先聚焦一个交易所做透，以后再扩展

## Tech Stack

### 架构：Web Console + Python 常驻后端

```
Browser / Mobile Browser
        │ HTTPS
        ▼
┌──────────────────────────────────────────────┐
│              Web App (React + TS)            │
│  - 仪表盘 / K 线图 / 信号面板                 │
│  - 币种列表管理 / 策略状态                    │
│  - LLM 分析结果展示 / 配置页                  │
└───────────────────┬──────────────────────────┘
                    │ REST + WebSocket/SSE
                    ▼
┌──────────────────────────────────────────────┐
│              FastAPI Server                  │
│  - Auth / API / WebSocket 推送                │
│  - 配置管理 / 查询接口 / 手动分析             │
└───────────────────┬──────────────────────────┘
                    │ in-process scheduler or worker
                    ▼
┌──────────────────────────────────────────────┐
│              Market Worker                   │
│  - ccxt 数据获取引擎                          │
│  - 策略计算引擎 (pandas-ta)                   │
│  - LLM 调用 (DeepSeek/通义/智谱)              │
│  - 信号检测 + 短信/邮箱推送                   │
│  - 数据存储 (PostgreSQL + TimescaleDB)        │
└──────────────────────────────────────────────┘
```

### 前端 (Web + React)
- **Vite + React + TypeScript** — Web 控制台，浏览器访问，不依赖本机常开
- **Recharts / Lightweight Charts** — K 线图和指标图表
- **Tailwind CSS** — 样式
- **Zustand** — 状态管理
- **WebSocket / SSE** — 实时推送最新价格、策略状态和信号

### 后端 (Python Server + Worker)
- **FastAPI** — HTTP/WebSocket 服务，对 Web 前端提供 API
- **ccxt** — 币安 API 数据获取（行情、K 线、资金费率、资金流）
- **pandas + pandas-ta** — 数据处理 + 技术指标计算（EMA、MACD、KDJ）
- **httpx** — 异步 HTTP 客户端（调用 LLM API）
- **APScheduler** — V1 定时任务调度；V2 可拆成 Celery/RQ/独立 worker
- **pydantic** — 数据模型校验
- **uvicorn/gunicorn + systemd/Docker** — 服务器常驻运行与进程守护

### 存储
- **PostgreSQL + TimescaleDB（V1 主存储）** — K 线、指标、资金费率、资金流、持仓量、多空比等时间序列数据存入 hypertable；策略模板、策略实例、挂载关系、状态机、信号、推送日志存入普通 PostgreSQL 表
- **Redis（可选缓存）** — 缓存最新价格、最近 K 线、最新策略状态和 WebSocket/SSE 推送快照；服务重启后可从 PostgreSQL/TimescaleDB 重建
- **Parquet / 对象存储（V1.5 冷归档）** — 用于长期历史 K 线、回测数据导出、离线分析，不参与实时策略判断
- **ClickHouse / QuestDB（V2 可选分析库）** — 当数据规模扩展到高频 tick、订单簿、跨多年多币种回放和大规模聚合分析时，再作为 OLAP/时序分析库引入
- **API Key 加密** — V1 使用服务器环境变量或本机密钥文件保存主密钥，对 PostgreSQL 中的交易所/LLM Key 做应用层加密；不在前端返回明文

### 推送
- **短信**: 阿里云短信 / Twilio API
- **邮箱**: SMTP (Gmail/QQ邮箱)
- **电话**: V2 实现

### LLM
- **V1: DeepSeek API** 作为默认 provider，接口设计为 provider-agnostic
- V1 同时支持：通义千问（DashScope API）、智谱 GLM（ZhipuAI API）
- V2 再添加 OpenAI/Claude 支持
- Prompt 模板：信号触发时，自动组装资金流数据 + K 线形态 + 指标状态，请求 LLM 分析

### LLM Prompt 模板骨架

```
你是合约交易分析顾问。以下是当前市场数据：

【币种】{symbol}
【持仓周期】{holding_timeframe}
【执行周期】{execution_timeframe}
【周期筛选置信度】{timeframe_confidence}
【信号类型】{signal_type}（多头/空头）
【EMA 状态】EMA9={ema9}, EMA21={ema21}, EMA55={ema55}，排列={alignment}
【MACD 状态】MACD={macd}, Signal={signal}, Histogram={hist}，零轴方向={zero_cross_dir}
【KDJ 状态】K={k}, D={d}, J={j}
【资金费率】{funding_rate}，趋势={funding_trend}
【24h 资金流】净流入={net_inflow}，多头占比={long_ratio}%
【大户持仓】多头={top_long_ratio}%，空头={top_short_ratio}%
【相邻周期状态】上级={upper_tf_status}，下级={lower_tf_status}

请分析：
1. 资金流是否支撑当前信号方向？
2. 大户持仓是否有异常变化？
3. 综合判断：做多 / 做空 / 观望
4. 核心理由（2-3 句话）
5. 风险提示
```

## 数据流架构

```
币安 API (ccxt)
    │
    ▼
┌──────────┐     ┌──────────┐
│ K线数据   │────▶│ Timescale│ (hypertable: symbol + timeframe + ts)
│ 资金费率  │     │ hypertable│
│ 资金流    │     └──────────┘
│ 持仓分布  │
└────┬─────┘
     │
     ▼
┌──────────┐
│ 策略引擎  │ ← pandas-ta 计算 EMA/MACD/KDJ
│          │ ← 三周期过滤链
│          │ ← 无效化规则检测
└────┬─────┘
     │ 候选周期
     ▼
┌──────────┐
│ 周期筛选器│ ← 资金流/持仓/动量/噪声综合评分
└────┬─────┘
     │ 主周期信号触发
     ▼
┌──────────┐     ┌──────────┐
│ LLM 分析  │────▶│ 信号记录  │ → PostgreSQL
│ 资金流    │     │ 推送记录  │
│ 持仓分析  │     └──────────┘
└────┬─────┘
     │
     ▼
┌──────────┐
│ 推送引擎  │ → 短信 / 邮箱 / 电话
└──────────┘
```

## 数据采集频率

| 数据类型 | 频率 | 说明 |
|---------|------|------|
| K 线数据 | 1 分钟 | 1m/5m/15m/1h/4h/1d 多周期 |
| 实时价格 | WebSocket | 币安 WebSocket 推送，< 1 秒延迟 |
| 资金费率 | 5 分钟 | 币安 8 小时结算，5 分钟检查一次 |
| 资金流 | 1 分钟 | 24h 资金流入流出 |
| 持仓分布 | 5 分钟 | 大户/散户持仓比例 |

**API 调用预算**：20 币种 × 6 周期 = 120 次/分钟（K线）+ 20 × 2（资金费率+持仓）+ 20（资金流）≈ 180 次/分钟。币安限制 1200 req/min，有余量。

## K 线与时序数据存储结构

V1 主存储使用 PostgreSQL + TimescaleDB，不再以 Feather 文件作为主库。

### TimescaleDB hypertable

| 表 | 时间列 | 维度 | 用途 |
|----|--------|------|------|
| `market_klines` | `open_time` | `exchange`, `market_type`, `symbol`, `timeframe` | K 线 OHLCV 数据 |
| `market_indicators` | `ts` | `symbol`, `timeframe`, `indicator_name` | EMA/MACD/KDJ/ATR 等指标结果 |
| `funding_rates` | `ts` | `symbol` | 资金费率、下一结算时间 |
| `market_flows` | `ts` | `symbol` | 主动买卖比、资金流、净流入等 |
| `open_interest_snapshots` | `ts` | `symbol` | 持仓量 OI 与变化率 |
| `long_short_ratios` | `ts` | `symbol`, `ratio_type` | 大户多空比、全市场多空比 |

K 线表建议唯一键：

```sql
UNIQUE (exchange, market_type, symbol, timeframe, open_time)
```

这样 Worker 重启后重复补 K 线时可以使用 upsert，避免重复数据。

### 冷归档与离线回测

- V1 实时查询只读 TimescaleDB
- V1.5 可以定期把历史 K 线导出为 Parquet，用于离线回测、压缩归档和大规模分析
- Feather 可保留为开发调试或单次导出格式，不再承担主数据源职责

## PostgreSQL 表结构（核心表）

- `watchlist` — 监控币种列表
- `strategy_templates` — 策略模板定义（周期槽位、条件组、默认参数、输出规则）
- `strategy_instances` — 由模板生成的具体监控策略（参数、周期组合、启用状态）
- `strategy_bindings` — 策略挂载关系（策略实例批量绑定到 N 个币种、市场、推送规则、参数覆盖）
- `strategy_states` — 策略运行状态（按 `strategy_instance_id + symbol` 独立记录未命中、列入监控、等待触发、已触发、失效、冷却）
- `strategies` — 兼容保留的策略配置表，后续可迁移到模板/实例结构
- `timeframe_decisions` — 交易/持仓周期筛选记录（候选周期、主周期、执行周期、评分明细、置信度、决策理由）
- `signals` — 信号记录（币种、周期、信号类型、触发时间、LLM 分析结果）
- `push_logs` — 推送记录
- `exchange_config` — 交易所配置（API Key 加密存储）
- `llm_config` — LLM 配置（provider、API Key 加密存储、prompt 模板）

## 前后端 API 契约

FastAPI 后端部署在服务器上，对外通过 HTTPS 提供 API；前端静态资源可由同一服务托管，也可由 Nginx/Caddy 托管后反向代理到 FastAPI。

### 核心端点

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| GET | /api/health | 健康检查 | - | `{status: "ok", uptime: N}` |
| GET | /api/watchlist | 获取监控列表 | - | `[{symbol, timeframe[], active}]` |
| POST | /api/watchlist | 添加监控币种 | `{symbol, timeframe[]}` | `{ok: true}` |
| DELETE | /api/watchlist/{symbol} | 删除监控币种 | - | `{ok: true}` |
| GET | /api/kline/{symbol}/{tf} | 获取 K 线数据 | ?limit=N | `[{ts, o, h, l, c, v}]` |
| GET | /api/indicators/{symbol}/{tf} | 获取指标数据 | ?limit=N | `{ema: {...}, macd: {...}, kdj: {...}}` |
| GET | /api/funding/{symbol} | 获取资金费率 | - | `{rate, next_time, trend}` |
| GET | /api/flow/{symbol} | 获取资金流 | - | `{net_inflow, long_ratio, top_ratio}` |
| GET | /api/signals | 获取最近信号 | ?limit=N | `[{symbol, tf, type, ts, llm_analysis}]` |
| GET | /api/strategy/status | 策略状态 | - | `[{symbol, tf, state, last_signal}]` |
| GET | /api/strategy/templates | 获取策略模板列表 | - | `[{id, name, timeframe_slots, condition_groups}]` |
| POST | /api/strategy/templates | 创建策略模板 | `{name, slots, conditions}` | `{id}` |
| GET | /api/strategy/instances | 获取监控策略列表 | - | `[{id, template_id, name, active}]` |
| POST | /api/strategy/instances | 从模板生成监控策略 | `{template_id, params, timeframes}` | `{id}` |
| POST | /api/strategy/instances/{id}/mount | 批量挂载策略到市场 | `{symbols, notify_rules, overrides}` | `{ok: true, mounted: N}` |
| POST | /api/strategy/instances/{id}/unmount | 暂停策略监控 | - | `{ok: true}` |
| GET | /api/timeframe/decisions | 获取最近周期筛选结果 | ?symbol=&limit=N | `[{symbol, holding_tf, execution_tf, confidence, reason}]` |
| POST | /api/timeframe/evaluate | 手动触发周期筛选 | `{symbol}` | `{holding_timeframe, execution_timeframe, confidence, score_breakdown}` |
| POST | /api/strategy/reload | 热加载策略 | - | `{ok: true}` |
| POST | /api/llm/analyze | 手动触发 LLM 分析 | `{symbol, tf, signal_type}` | `{analysis, recommendation, risk}` |
| GET | /api/config | 获取配置 | - | `{exchange, llm, notifier, indicators, timeframes}` |
| PUT | /api/config | 更新配置 | `{...}` | `{ok: true}` |
| GET | /api/config/indicators | 获取指标参数 | - | `{ema: [9,21,55], macd: [12,26,9], kdj: [9,3,3]}` |
| PUT | /api/config/indicators | 更新指标参数 | `{ema: [...], macd: [...], kdj: [...]}` | `{ok: true}` |
| GET | /api/config/timeframes | 获取监控周期 | - | `["1m","5m","15m","1h","4h","1d"]` |
| PUT | /api/config/timeframes | 更新监控周期 | `["1m","5m",...]` | `{ok: true}` |

### 错误响应格式

```json
{"error": "ERROR_CODE", "message": "描述", "detail": {}}
```

## 服务生命周期管理

- FastAPI + Market Worker 作为服务器常驻进程运行，V1 推荐 Docker Compose 或 systemd 管理
- 健康检查：每 10 秒 GET /api/health，连续 3 次失败由 Docker/systemd 自动重启
- 后台任务：APScheduler 随服务启动，负责行情采集、指标计算、周期筛选、信号检测、LLM 分析和推送
- 优雅关闭：服务收到 SIGTERM 后停止新增任务，等待当前数据写入和推送任务完成，再退出
- 单实例锁：V1 只允许一个 Market Worker 执行同一类采集/策略扫描任务；数据库层通过唯一键 + upsert 保证 K 线补全幂等

## 错误处理

| 场景 | 处理方式 |
|------|---------|
| 币安 API 宕机 | 指数退避重试（1s → 2s → 4s → ... → 60s），UI 显示"数据源断开" |
| LLM API 超时 | 30 秒超时，失败后信号仍推送（不含 LLM 分析），标记"LLM 分析失败" |
| 数据库写入失败 | 写入失败进入重试队列，保留采集游标，恢复后按 symbol/timeframe/open_time 幂等补写 |
| 内存超限 | 超过 1.5GB 时自动清理最早周期的 DataFrame，保留最近 3 个周期数据 |
| 网络断开 | WebSocket 自动重连，REST API 切换到轮询模式 |

## 核心模块

### 1. 数据采集引擎 (data_fetcher)
- ccxt 异步获取币安合约数据
- 币安 WebSocket 实时价格推送
- APScheduler 定时调度
- 数据落盘（PostgreSQL + TimescaleDB upsert）
- 异常重试 + 断线重连 + 退避策略
- **ccxt 限制说明**：币安资金流端点（topTraderLongShortAccountRatio, takerlongshortRatio, globalLongShortAccountRatio）可能不在 ccxt 标准接口中，需通过 `ccxt.binance.fapiPublicGet()` 或直接 HTTP 调用币安 API

### 2. 策略模板与组装器 (strategy_builder)
- 管理 2 周期、3 周期等可复用策略模板
- 提供可变数量的语义周期槽位配置：方向周期、结构周期、持仓周期、触发周期、确认周期
- 提供多类型条件积木库：指标信号、结构信号、资金流信号、周期信号、时间条件、风险过滤、无效化条件
- 将模板 + 参数 + 周期组合生成具体 `strategy_instance`
- 将策略实例批量挂载到 N 个币种和推送规则，形成可运行的监控策略

### 3. 策略引擎 (strategy_engine)
- Python 代码定义策略（可热加载）
- 读取 `strategy_instances` 并按模板条件执行监控
- 按 `strategy_instance_id + symbol` 维护独立策略状态机：未命中 → 列入监控 → 等待触发 → 已触发 → 失效/冷却
- 二周期/三周期过滤链实现
- EMA 首次金叉/死叉检测
- MACD 零轴穿越检测
- 均线排列破坏检测（无效化规则）
- 相邻周期联动监控

### 4. 交易/持仓周期筛选器 (timeframe_selector)
- 汇总所有发生首次交叉的候选周期，不默认选择最小周期
- 结合资金流、持仓分布、EMA 排列、MACD/KDJ、成交量、ATR 和相邻周期一致性打分
- 输出 `holding_timeframe`、`execution_timeframe`、方向、置信度、评分明细和自然语言理由
- 低置信度或周期冲突时输出 `NO_TRADE / 观望`，避免在混乱周期里强行给信号
- 筛选结果写入 PostgreSQL，前端展示本次为什么选 4h 而不是 1h/1d

### 5. LLM 分析模块 (llm_analyzer)
- 信号触发时自动调用
- 组装策略模板、周期筛选结果、资金流数据、K 线形态、指标状态
- 多 provider 支持（OpenAI/Claude/DeepSeek）
- 结果缓存 + 展示

### 6. 推送引擎 (notifier)
- 短信 + 邮箱两通道（V2 加电话）
- 推送优先级配置：强信号（均线首次交叉+大周期确认）→ 短信；弱信号（仅 MACD 信号）→ 邮箱
- 推送频率限制：同一币种同方向信号 5 分钟内不重复推送

### 7. 前端 UI
- 实时仪表盘：多币种 K 线 + 指标叠加
- 信号面板：最新信号 + LLM 分析结果
- 周期筛选面板：展示候选周期、最终持仓周期、执行周期、置信度和评分明细
- 策略组装页面：创建模板、配置周期槽位、填入条件、生成监控策略、挂载币种
- 币种管理：添加/删除监控币种
- 策略状态：当前各周期策略状态一览
- V2 功能：历史回放、K 线图时间轴拖拽

## Approaches Considered

### Approach A: Web Console + Python Server/Worker（已选）
- Summary: React Web 控制台负责查看和配置，Python FastAPI + Market Worker 在服务器 24h 常驻运行
- Effort: M
- Risk: Low
- Pros: 不依赖个人电脑常开，手机/电脑都能访问，后端监控和前端展示边界清晰
- Cons: 需要处理登录、HTTPS、服务器部署、密钥保护和进程守护
- Reuses: ccxt、pandas-ta、FastAPI、React 生态

### Approach B: Python 守护进程 + Bot/短信通知
- Summary: 后端只做常驻监控和推送，通过 Telegram/企业微信/短信/邮件查看信号
- Effort: S
- Risk: Med
- Pros: V1 最快跑通，几乎不需要前端，推送链路天然优先
- Cons: K 线、评分明细、策略配置和历史回看体验差，后续大概率还要补 Web 控制台
- Reuses: ccxt、pandas-ta、FastAPI/CLI

### Approach C: Tauri + Python Sidecar
- Summary: Tauri 做原生桌面壳 + React UI，Python sidecar 处理所有业务逻辑
- Effort: M
- Risk: Med
- Pros: 原生体验好，个人本机使用轻量，策略调试方便
- Cons: 不解决个人电脑必须常开的问题，和 24h 服务器监控目标冲突
- Reuses: ccxt、pandas-ta、FastAPI 生态

## Recommended Approach

方案 A: Web Console + Python Server/Worker。核心监控引擎在服务器 24h 常驻运行，Web 控制台只负责查看、配置和手动分析。PostgreSQL + TimescaleDB 作为 V1 主数据层，交易/持仓周期筛选器作为策略引擎里的关键裁判，先解决多周期首次交叉混乱，再触发 LLM 分析和推送。

## Open Questions

- 策略模板表达方式：V1 是用表单式配置（更适合页面操作），还是用 JSON/YAML/Python DSL（更灵活但门槛高）？
- 条件组关系：V1 是否只支持 AND/OR 两种组合，还是需要支持嵌套条件（例如 A AND (B OR C)）？
- 策略状态机粒度：是否需要区分"列入监控"、"等待触发"、"已触发"、"失效"、"冷却"，还是 V1 只保留"未触发/已触发/失效"？
- 二周期和三周期是否共用同一模板引擎：建议共用，通过周期槽位数量不同实现，而不是写两套策略逻辑
- 策略热加载机制：改完策略代码后是否需要重启 Market Worker？（当前设计：POST /api/strategy/reload 支持热加载）
- K 线数据保留策略：TimescaleDB 中不同周期保留多久？是否启用压缩和连续聚合？（建议：1m 保留 30-90 天，5m/15m 保留 180 天，1h/4h/1d 长期保留）
- 半自动下单功能：V1 是否包含？还是先只做监控+推送+分析？（建议：V1 不包含，V2 加）
- EMA "首次"定义中的分离 K 线数 N：默认 10 是否合适？需要用户确认
- 周期筛选器评分权重：资金流 30%、趋势结构 25%、动量 20%、周期一致性 15%、风险惩罚 10% 是否适合，需要用历史信号回放校准
- 币安资金流端点的 ccxt 支持情况：需要实际验证

## Success Criteria

- 能同时监控 20+ 币种，WebSocket 价格延迟 < 1 秒，K 线数据延迟 < 60 秒
- 能通过策略模板创建 2 周期/3 周期监控策略，并挂载到指定币种运行
- 双周期双均线策略能完整跑通：大周期均线多头 → 列入监控 → 小周期金叉 → 触发提醒
- 策略信号从触发到推送通知 < 30 秒
- 多周期同时首次交叉时，周期筛选器能在 < 3 秒内输出持仓周期、执行周期、置信度和评分理由
- LLM 分析（含 API 调用）< 15 秒
- 历史数据查询 < 500ms
- 服务内存占用 < 1GB（Market Worker 是主要内存消耗，20 币种 × 6 周期 DataFrame + 指标列约 500-800MB）
- 服务器常驻运行 24h+ 无崩溃，进程异常退出后能自动重启

## Distribution Plan

- V1 使用 Docker Compose 或 systemd 部署到单台 VPS
- Nginx/Caddy 提供 HTTPS 和反向代理
- 前端构建为静态文件，由 Nginx/Caddy 或 FastAPI 托管
- 后端使用 `.env` 管理服务器环境变量，PostgreSQL/TimescaleDB 数据目录挂载到持久化磁盘
- 暂不需要复杂 CI/CD，先用手动部署脚本；V2 再接 GitHub Actions

## Dependencies

- Python 3.11+
- Node.js 18+（前端构建）
- PostgreSQL 16+ + TimescaleDB
- Redis（可选，用于最新行情与状态缓存）
- VPS/云服务器（2C4G 起步，建议独立磁盘保存数据）
- 域名 + HTTPS 证书（Caddy 可自动签发）
- 币安 API Key（只读权限即可，交易需交易权限）
- LLM API Key（至少一个国内 provider：DeepSeek/通义千问/智谱GLM）

## Next Steps

1. 搭建 FastAPI + Market Worker 基础框架
2. 搭建 React Web 控制台项目骨架
3. 实现 PostgreSQL + TimescaleDB 数据层
4. 实现策略模板与组装器（周期槽位 + 条件积木 + 策略实例）
5. 实现双周期双均线模板原型（大周期趋势 + 小周期触发）
6. 实现策略挂载与状态机（列入监控、等待触发、已触发、失效、冷却）
7. 实现策略引擎核心逻辑（首次交叉扫描 + 二/三周期过滤链）
8. 实现交易/持仓周期筛选器（候选周期评分 + 主周期选择）
9. 实现 LLM 分析模块
10. 实现推送引擎
11. 实现前端 UI（仪表盘 + 策略组装 + 信号面板 + 周期筛选面板）
12. 集成测试 + 服务器部署

## The Assignment

先用 ccxt 写一个 Python 脚本，获取币安合约的 BTCUSDT 的 1m/5m/15m/1h/4h/1d K 线数据 + 资金费率 + 资金流数据，写入 PostgreSQL + TimescaleDB，然后用 pandas-ta 计算 EMA/MACD/KDJ 指标并写回 `market_indicators`。下一步先做一个"双周期双均线趋势模板"原型：大周期 EMA 多头后列入监控，小周期 EMA 金叉后触发提醒。这个原型跑通后，再把它抽象成策略模板/条件积木/策略挂载系统，最后加周期筛选器解决多周期同时触发的问题。

## What I noticed about how you think

- 你的策略逻辑比大多数人想的深——"首次金死叉"不是随便说说的，你明确知道必须是首次，中间有无效化规则，有均线排列破坏的检测。这不是从书上抄的，是从实战里提炼的。
- 你对存储有性能意识——主动问 Feather 会不会更快，说明你关心数据层的实际效率，不是随便选个方案就上。
- 你选了 Python 代码定义策略而不是可视化配置——说明你更看重灵活性和精确控制，而不是"看起来方便"。
- 你要实时仪表盘 + 推送双通道——说明你既想盯着屏幕感受市场节奏，也不想因为离开屏幕错过信号。这是认真做交易的人的心态。
